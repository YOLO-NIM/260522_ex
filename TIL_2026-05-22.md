# TIL | 2026-05-22

오늘은 JavaScript 기초 문법을 반복문부터 배열 메서드까지 순서대로 실습했다.

## 오늘 배운 내용

- `while`, `for`로 반복문을 작성하고, `break`와 `continue`로 흐름을 제어했다.
- `for-in`, `for-of`의 차이를 익혔다.
  - `for-in`: key를 순회
  - `for-of`: value를 순회
- 함수 선언문, 함수 표현식, 화살표 함수의 차이를 정리했다.
- 매개변수 기본값, rest parameter, `arguments` 객체를 사용해봤다.
- 호이스팅에서 `var`와 `function` 선언이 먼저 끌어올려지는 특징을 확인했다.
- `let`, `const`, `var`의 스코프 차이와 블록 스코프 개념을 정리했다.
- 클로저를 통해 함수가 외부 변수를 기억하는 방식을 실습했다.
- 객체와 배열의 구조 분해 할당을 사용했다.
- 객체/배열의 얕은 복사와 참조 공유의 차이를 확인했다.
- `Object.keys`, `Object.values`, `Object.entries`로 객체를 순회했다.
- 배열의 `push`, `pop`, `shift`, `unshift`, `splice` 동작을 확인했다.

## 기억할 점

- `for`문 안에서는 반복 변수의 변화를 직접 관리해야 한다.
- 객체는 기본적으로 key-value 구조라서 `for-in`과 `Object.keys()` 계열이 잘 맞는다.
- 배열은 순서가 중요하므로 `for-of`와 배열 메서드 조합이 자주 쓰인다.
- 구조 분해 할당은 코드가 짧아지지만, 원본과 복사본의 참조 관계는 따로 확인해야 한다.
- 클로저는 상태를 숨기면서 유지하는 데 유용하다.

## 오늘의 정리

JS는 문법을 외우는 것보다, `scope`, `hoisting`, `reference`, `iteration`이 어떻게 연결되는지 이해하는 게 중요했다.
특히 배열과 객체는 "값을 복사한 것인지, 참조를 공유하는 것인지"를 구분하는 습관이 필요하다.

## 다음에 해볼 것

- `map`, `filter`, `reduce` 같은 배열 고차 함수 실습
- 객체 깊은 복사와 얕은 복사 비교
- 함수형 스타일로 간단한 데이터 처리 예제 작성
